const course = {
    title: "JavaScript",
    duration: 3,
    price : 200,
}  

//course.title

const {title} = course   // Destructuring, if you want to get the title property from the course object then you can use destructuring like this
console.log(title) // JavaScript