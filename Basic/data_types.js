"use Strict" // treat all code  with newer version ........not necessary to declare beacuse it auttomaticaaly goes to new version of js

//alert("Hello World") //we are using nodejs not browser........thats why it shows error

let name = "Dnyana"    // string = ""
let age = 22
let isStudent = true   // boolean = true/false

let acc = null // null = no value

let state; // undefined = no value(not assigned yet)

console.log(typeof name); 
console.log(typeof age);