// Array is the collection of data-types..we can add different datatype in an Array
// wheras string is sequence of characters to represent text

// Array is mutable where we can aaply any methods in it without creating new array
// string is immutable, we need to create a new string for doing any changes in itslef

let food = ['pizza', 'burger', 'pasta'];
console.log(food); // Initial array
// food.push('sushi'); // Adding an item to the array
// console.log(food); 

// food.pop(); // Removing the last item from the array
// console.log(food);
            

            //shift() & unshift() method 
            // shift() is the method which is used to remove the first element in array 
            // unshift () method is used used to add first element in array 


console.log(food.toString()); // Converting array to string, here it will return a new string where array is converted to string
console.log(food); // The original array remains unchanged
